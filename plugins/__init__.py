from plugins.malik2.buttons import HSTN,  HSTNN, NOTFOUN, GROUP_RULES, TFRADE
from info import BOT_USERNAME, DEL_SECOND
from plugins.malik.extra import INST, IYGL, YTILK, MQTT, PPI, SMART_PIC, REQUEST_ADMIN 
from Script import MNTFN 
from utils import get_shortlink 




class malik(object):
      hstn = HSTN
      hstnn = HSTNN
      u_name = BOT_USERNAME 
      int_link = IYGL
      smart_pic = SMART_PIC 
      delete = DEL_SECOND
      notfoun = REQUEST_ADMIN 
      mntf = MNTFN
      ntatm =  PPI
      ytilk = YTILK
      mqtt = MQTT
      inst = INST
      group_rules = GROUP_RULES 
      tfarde = TFRADE

